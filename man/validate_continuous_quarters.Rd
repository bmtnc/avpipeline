% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_continuous_quarters.R
\name{validate_continuous_quarters}
\alias{validate_continuous_quarters}
\title{Validate Continuous Quarterly Series}
\usage{
validate_continuous_quarters(
  ticker_data,
  date_col = "fiscalDateEnding",
  row_num_col = "row_num",
  cleanup_cols = c("days_diff", "is_quarterly", "row_num")
)
}
\arguments{
\item{ticker_data}{Data frame containing a single ticker's time series data}

\item{date_col}{Character string specifying the column name containing fiscal date endings
(default: "fiscalDateEnding")}

\item{row_num_col}{Character string specifying the column name that contains row numbers
for filtering (default: "row_num")}

\item{cleanup_cols}{Character vector of column names to remove from the final output
(default: c("days_diff", "is_quarterly", "row_num"))}
}
\value{
Data frame containing the continuous quarterly series from the identified start
point onward, with cleanup columns removed. Returns empty data frame if no continuous
quarterly series is found.
}
\description{
Validates that a company has continuous quarterly reporting by generating
company-specific expected quarterly dates and comparing against actual reporting dates.
Uses the company's first reporting date to establish their fiscal quarter pattern.
}
