% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_phase2_result.R
\name{normalize_phase2_result}
\alias{normalize_phase2_result}
\title{Normalize a Single Phase 2 Worker Result into a Log Record}
\usage{
normalize_phase2_result(result, ticker, data_type)
}
\arguments{
\item{result}{Worker return value for one ticker}

\item{ticker}{character: Ticker the result belongs to}

\item{data_type}{character: Log data_type label}
}
\value{
tibble: Single-row pipeline log entry
}
\description{
Maps one worker return value onto the pipeline log schema. A worker can
return three shapes and each must be distinguishable in the log: a
well-formed record, a bare NULL, or a \code{try-error}.
}
\details{
A killed fork (OOM, segfault) surfaces as NULL under \code{mc.preschedule = TRUE}
— mclapply's default and what Phase 2 uses — with the whole scheduled chunk
NULLed and a "did not deliver a result" warning; it surfaces as \code{try-error}
only under \code{mc.preschedule = FALSE}. Both are logged as errors, since
recording them as "skipped" would let a mass worker kill read as a clean run.
}
\keyword{internal}
