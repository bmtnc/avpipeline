% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_phase2_log.R
\name{build_phase2_log}
\alias{build_phase2_log}
\title{Build Phase 2 Pipeline Log from Worker Results}
\usage{
build_phase2_log(results, tickers, data_type = "quarterly")
}
\arguments{
\item{results}{list: Per-ticker records from the Phase 2 worker}

\item{tickers}{character: Tickers processed, positionally matching results}

\item{data_type}{character: Log data_type label (default: "quarterly")}
}
\value{
tibble: Pipeline log with one row per ticker
}
\description{
Assembles the per-ticker pipeline log from the records returned by the
Phase 2 parallel workers.
}
\details{
Phase 2 processes tickers under \code{parallel::mclapply}, which forks. A forked
worker cannot append to a shared log — its mutations die with the fork — so
each worker returns its own outcome record and the parent assembles them
here. This is why Phase 2 cannot reuse the sequential \code{add_log_entry()}
accumulation that Phase 1's serial ticker loop relies on.
}
\keyword{internal}
