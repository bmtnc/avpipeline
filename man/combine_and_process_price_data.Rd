% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_and_process_price_data.R
\name{combine_and_process_price_data}
\alias{combine_and_process_price_data}
\title{Combine and Process Price Data}
\usage{
combine_and_process_price_data(existing_data, new_data, as_of_date)
}
\arguments{
\item{existing_data}{Data frame with existing cached price data (can be NULL)}

\item{new_data}{Data frame with newly fetched price data}

\item{as_of_date}{Date, timestamp for when the data was pulled}
}
\value{
A data frame with combined data and metadata columns
}
\description{
Combines existing cached data with newly fetched data and adds metadata columns
including row count, initial date, latest date, and as-of date for each ticker.
}
\examples{
\dontrun{
existing_data <- read_cached_price_data("cache/price_data.csv")
new_data <- fetch_multiple_tickers(c("AAPL", "GOOGL"))
combined_data <- combine_and_process_price_data(existing_data, new_data, Sys.Date())
}

}
